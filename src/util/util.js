/**
 * 工具库
 */

let util = {
  /**
   * 日期美化
   */
  beautifyDate() {

  },

  /**
   * 动态添加样式
   */
  style() {

  },

  /**
   * 获取滚动条高度
   */
  getScrollTop() {

  },

  /**
   * 返回Global变量中获取数据
   */
  g() {
    if (!window.__JSCMS_CONSTANT__) {
      window.__JSCMS_CONSTANT__ = {};
    }
    return window.__JSCMS_CONSTANT__;
  }
}

export default util;